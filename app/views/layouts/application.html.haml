!!!
%html{"ng-app" => @ng_app_name || true}
  %head
    %title MindSwarms
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = yield :head
    = csrf_meta_tags
  %body
    = yield
    - if Rails.env.development?
      / This is the livereload snippet - if docs are to be believed, when grunt rebuilds
      / this will trigger a browser reload with the new code
      :javascript
        document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1"></' + 'script>')
